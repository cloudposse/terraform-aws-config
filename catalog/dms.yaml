dms-replication-not-public:
  identifier: DMS_REPLICATION_NOT_PUBLIC
  description: >-
    Checks whether AWS Database Migration Service replication instances are public. The rule is NON_COMPLIANT if
    PubliclyAccessible field is true.
  inputParameters: {}
  enabled: true
  tags:
    catalog/dms: true
